---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: namespacemirrors.mirror.mirror.dev
spec:
  group: mirror.mirror.dev
  names:
    kind: NamespaceMirror
    listKind: NamespaceMirrorList
    plural: namespacemirrors
    shortNames:
    - nsm
    singular: namespacemirror
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: NamespaceMirror is the Schema for the namespacemirrors API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NamespaceMirrorSpec defines the desired state of NamespaceMirror
            properties:
              sourceNamespace:
                type: string
              targetNamespace:
                type: string
            required:
            - sourceNamespace
            - targetNamespace
            type: object
          status:
            description: NamespaceMirrorStatus defines the observed state of NamespaceMirror.
            properties:
              configMapsSynced:
                type: integer
              deploymentsSynced:
                type: integer
              lastSyncTime:
                type: string
              podsSynced:
                type: integer
              replicaSetsSynced:
                type: integer
              secretsSynced:
                type: integer
              servicesSynced:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
